import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// To run the bonus (JSX) version, comment out the line above and comment in the
// line below.
// import App from './AppBonus';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
